services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: calid
      POSTGRES_USER: calid
      POSTGRES_PASSWORD: calid
    ports:
      - "5432:5432"
    volumes:
      - calid-postgres:/var/lib/postgresql/data

volumes:
  calid-postgres:
